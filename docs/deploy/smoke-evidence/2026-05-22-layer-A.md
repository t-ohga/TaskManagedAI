# Layer A evidence summary (SP022-T09 prep Mac single-host smoke)

実施日時: 2026-05-22T03:06Z〜03:14Z (約 8 min)
worktree: `worktree-sp022-t09-prep-mac-smoke` (branched from `origin/main` `6dc48d8`)
host: macOS (Darwin 25.5.0)
python: 3.12.11 / uv / pnpm 10.18.0

## 結果

| # | task | command | 結果 | evidence |
|---|---|---|---|---|
| A-1a | uv sync | `uv sync` | ✅ exit 0、Resolved 78 packages | `A-1-uv-sync.log` |
| A-1b | pnpm install (frozen) | `pnpm install --frozen-lockfile` | ✅ exit 0、Already up to date | `A-1-pnpm-install.log` |
| A-2 | backend full pytest | `uv run pytest -x` | ✅ **3885 passed / 350 skipped / 26 warnings** (15.58s) | `A-2-pytest.log` |
| A-3a | backend ruff | `uv run ruff check backend tests` | ✅ **All checks passed!** (37 errors → fixed by per-file-ignore for test fixtures + mypy fix の副次効果) | `A-3-ruff-final.log` |
| A-3b | backend mypy | `uv run mypy backend` | ✅ **234 source files no issues** (5 errors → fixed by cast addition + unused-ignore removal) | `A-3-mypy-final.log` |
| A-4 | frontend full vitest | `pnpm vitest run` | ✅ **55 passed / 10 test files** (1.47s) | `A-4-vitest.log` |
| A-5a | frontend typecheck | `pnpm typecheck` | ✅ tsc --noEmit clean | `A-5-typecheck.log` |
| A-5b | frontend eslint | `pnpm exec eslint . --max-warnings=0` | ✅ 0 errors / 0 warnings | `A-5-eslint.log` |
| A-6 | alembic offline check | revision linearity + 32-char hard limit | ✅ Hard limit + linear chain PASS。Soft convention (<=30) 2 件違反は known drift | `A-6-offline-check.log`, `A-6-revision-id-check.log` |

## A-3 修正内容 (latent issue fix)

### mypy errors (5 件 → 0)
`scripts/taskhub_active_registry.py`:
- L1064 `return candidate` → `return cast("dict[str, Any]", candidate)` (`json.loads` Any narrowing)
- L1337 `return json.loads(raw)` → `return cast("dict[str, Any]", json.loads(raw))` (同上)
- L75/78/79: `# type: ignore[import-not-found]` 削除 (型 stubs 解決可能のため unused-ignore)
- `from typing import` に `cast` を追加

### ruff errors (37 件 → 0)
`pyproject.toml [tool.ruff.lint.per-file-ignores]` に追加:
- `tests/scripts/test_taskhub_active_registry.py = ["E501"]` (9 件 long canonical signed JSON literal)
- `tests/scripts/test_taskhub_destructive_lock.py = ["E501"]` (1 件 long literal)
- `tests/scripts/test_taskhub_restore_orchestrator.py = ["ANN001", "ANN202", "S104", "S108"]` (26 件 fake_run callable + test fixture security pattern)

production code は影響を受けない (test fixture per-file ignore のみ)。

## A-6 known drift (soft convention violation、別 PR で fix 予定)

| revision | chars | hard limit (32) | soft convention (<=30) |
|---|---|---|---|
| `0009_artifacts_context_snapshots` | 32 | ✅ PASS | ⚠️ violation |
| `0015_artifact_prohibited_keys_21` | 32 | ✅ PASS | ⚠️ violation |

alembic_version.version_num varchar(32) hard limit は満たすため migration apply には影響なし。
rename は destructive operation (migration history change) のため本 plan scope 外、別 PR で対応。

## A-6 後続 (Layer B で実施)

実 DB upgrade test (`alembic upgrade head` on fresh DB) は Mac local PostgreSQL が不在のため Layer B (docker compose up) の中で実施:
- `docker compose up -d postgres` で local DB 立ち上げ
- `docker compose exec api uv run alembic upgrade head` で実 apply 確認

## 累計検証

- backend: 3885 tests + 234 mypy files + ruff clean = **all green**
- frontend: 55 vitest + tsc + eslint = **all green**
- migration: 18 revisions linear + hard limit PASS

## Critical suite-by-suite re-verify (Layer A 品質補強、2026-05-22T03:21Z 追加実施)

各 P0 critical test suite を独立に verify、suite ごとの evidence 確立:

| suite | passed | skipped | scope | autonomous verify 可? |
|---|---:|---:|---|---|
| `tests/security/` | 136 | 34 | secret canary / preflight / forbidden path / 21 prohibited key cross-surface | ✅ pure function path |
| `tests/eval/` | **1275** | 4 | Hard Gates 7 (AC-HARD-01〜07) + Quality KPIs 5 (AC-KPI-01〜05) pure path | ✅ pure function path |
| `tests/agent_runtime/` | 2 | 5 | AgentRun 16 状態 + state machine + repository | ⚠️ 大半 skipped = 実 DB 必要 (Layer B scope) |
| `tests/db/` | 28 | 134 | tenant boundary / 複合 FK / migration constraint | ⚠️ 大半 skipped = 実 DB 必要 (Layer B scope) |

**autonomous で verify 可能な範囲は完全 PASS**。skip された DB-dependent test は Layer B (docker compose up 後) で実施。

## Layer A coverage clarity (透明性)

| 検証対象 | autonomous OK? | 根拠 |
|---|---|---|
| backend pure function logic (validator / data class / state machine pure path) | ✅ | 3885 pytest PASS |
| backend lint / type (ruff / mypy) | ✅ | clean |
| frontend DOM rendering (component tests with mock) | ✅ | 55 vitest PASS |
| frontend type / lint | ✅ | tsc + eslint clean |
| alembic revision linear / hard limit | ✅ | offline check |
| alembic 実 DB apply (`upgrade head` on fresh DB) | ⚠️ Layer B | local PostgreSQL なし、docker compose 起動が前提 |
| backend actual DB integration (sqlalchemy session / repository contract) | ⚠️ Layer B | 大半 skipped |
| frontend ↔ backend wiring (Eval Dashboard live fetch 等) | ⚠️ Layer C | docker stack 起動が前提 |
| docker compose 5 service healthy | ⚠️ Layer B | user 実機 |
| taskhub CLI 実 backup/restore | ⚠️ Layer C | user 実機 (subprocess mock のみ test 済) |
| golden flow Ticket→PR smoke | ⚠️ Layer C | docker stack + e2e fixture |

**Layer A 全 6 件 PASS + critical suite 補強 verify 完了。Layer B (Mac docker compose smoke) + Layer C (機能 smoke + CLI) へ進行可。**
