{
  "_comment": "TaskManagedAI MCP Server — stdio transport. DB接続はenv.TASKMANAGEDAI_DATABASE_URLで127.0.0.1に向ける (host上で実行されるため Docker internal hostname は不可)。",
  "mcpServers": {
    "taskmanagedai": {
      "command": "uv",
      "args": ["run", "python", "-m", "backend.app.mcp"],
      "env": {
        "TASKMANAGEDAI_DATABASE_URL": "postgresql+asyncpg://taskmanagedai:taskmanagedai_local_smoke_pwd@127.0.0.1:5432/taskmanagedai"
      }
    }
  }
}
