# AC-KPI-02 private holdout

This split is intentionally empty in the public repository per `.claude/rules/testing.md §10` Anti-Gaming rules.

Encrypted holdout values (if any) live in an external eval vault and are not used for prompt, policy, or implementation tuning. `expected_count: 0` in `manifest.json` enforces emptiness.
