# AC-KPI-05 Private Holdout

Private holdout fixtures are intentionally absent from this repository skeleton.

When added, this split must keep expected aggregate values outside the policy and prompt tuning path. Fixture refreshes are append-only and must preserve the immutable index contract in `manifest.json`.

